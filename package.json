{
  "scripts": {
    "start": "wrangler dev",
    "publish": "wrangler publish"
  },
  "devDependencies": {
    "wrangler": "^2.20.0"
  },
  "template": {
    "mergers": [
      {
        "type": "TOML",
        "pattern": "wrangler.toml",
        "options": {
          "messagePrefix": "chore(wrangler): ",
          "expand": true,
          "mergeHints": {
            "*": {
              "overwrite": false
            }
          }
        }
      }
    ]
  }
}
