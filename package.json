{
  "scripts": {
    "start": "wrangler dev",
    "publish": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.9.1"
  },
  "template": {
    "mergers": [
      {
        "type": "TOML",
        "pattern": "wrangler.toml",
        "options": {
          "messagePrefix": "chore(wrangler): ",
          "expand": true,
          "mergeHints": {
            "*": {
              "overwrite": false
            }
          }
        }
      }
    ]
  }
}
